// const arra = [1,2,[3,4, [5,6]],7];
// function flatten (array) {
//   return array.reduce((acc, it) => {
//     if(Array.isArray(it)){
//       // return [...acc, ...flatten(it)];
//       return acc.concat(flatten(it));
//     }
//     acc.push(it);
//     return acc;
//   }, []);
// }
// console.log(flatten(arra));

// console.log('cikl',arg,filmList)
//           console.log('presenter',this.#popupPresenter)

// this.#popupPresenter.forEach((pres) => {
// this.destroy()
// pres.forEach((it) => console.log('it',it));
// remove(pres)
// this.#popupPresenter.clear();
// this.#clearFilmList()
//         this.#popupPresenter.forEach((presenter) => remove(presenter));
// this.#popupPresenter.clear();
// this.#numberOfDisplays = NUMBER_OF_DISPLAYS;
// remove(this.#showMoreButton);

// console.log('presenter  1',this.#popupPresenter)
// pres.destroy()
//   // this.destroy(pres)
// this.#popupPresenter.clear();
//   // this.#numberOfDisplays = NUMBER_OF_DISPLAYS;
// console.log('prees', pres)
// });
// console.log('popupPresenter1', this.#popupPresenter)
// // this.#cardComponent.destroy(this.#cardComponent, this.#cardPopupComponent);
// console.log('cardComponent',this.#cardComponent)
